---
- name: conditions 
  hosts: local
  connection: local
  vars:
    IS_Live: false
  tasks:
   - name: boolean values
     ansible.builtin.debug:
       msg: "This is printed if IS_LIVE is true"
     when: IS_Live